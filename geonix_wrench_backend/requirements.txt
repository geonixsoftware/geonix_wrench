# Security-relevant pins. pip-audit reported 19 known vulnerabilities across
# five of these on 2026-08-19; the versions below are the fixed ones.
#
#   python-multipart  7 advisories, and it parses every audio and logo upload
#                     this API accepts — the most exposed of the set.
#   starlette         9 advisories. Pinned explicitly because fastapi is what
#                     drags it in, and fastapi 0.115 capped it below the fixed
#                     versions (<0.39) — which is why fastapi moves too.
#
# Re-run the check before a release:
#   pip-audit -r requirements.txt --no-deps
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.30.6
python-multipart==0.0.31
faster-whisper==1.0.3
anthropic==0.40.0
httpx==0.27.2
reportlab==4.2.2
pillow==12.3.0
# Shop logo uploads: SVG is parsed into a reportlab drawing (no native
# rasteriser needed), HEIC/HEIF is what iPhones shoot.
svglib==1.5.1
pillow-heif==1.3.0
pydantic==2.9.2
python-dotenv==1.2.2
firebase-admin==6.5.0
stripe==15.5.0
pytest==9.0.3
