Geonix Wrench - what still needs doing before release
Written 2026-08-15. Revised 2026-08-17.

Ordered by what blocks a release, not by effort. Items marked [BLOCKER] mean a
paying customer would hit a broken or unsafe path on day one.

Everything left in this file needs credentials, an account, money, or a
force-push nobody should do on someone else's behalf. The code-side work is
done — see DONE at the bottom.


================================================================================
BLOCKERS
================================================================================

[BLOCKER] A real user's data is public in git history
    geonix_wrench_backend/geonix_wrench.db is untracked now, but the blob is
    still in commits d9cdbc6 and aa83443 — and BOTH are ancestors of the pushed
    branch on geonixsoftware/geonix_wrench, which is a PUBLIC repo. It holds one
    users row: firebase_uid, email, handle.
    The earlier note here reasoned this could wait because "this repo has no
    remote to recover from". That stopped being true when the remote was added.
    -> Purge it, from the repo root:
         git bundle create ../backup.bundle --all      # do this first
         FILTER_BRANCH_SQUELCH_WARNING=1 git filter-branch --force \
           --index-filter 'git rm --cached --ignore-unmatch \
             geonix_wrench_backend/geonix_wrench.db' \
           --prune-empty --tag-name-filter cat -- --all
         git reflog expire --expire=now --all && git gc --prune=now --aggressive
         git push --force --all && git push --force --tags
    -> Rewriting moves every commit SHA, so re-tag v1.0.0 afterwards.
    -> GitHub keeps unreachable objects reachable by direct SHA URL. Ask GitHub
       Support to purge the cached views, and treat that email as leaked
       regardless.

[BLOCKER] Rotate the three exposed credentials
    A live Stripe secret key (sk_live_), the Stripe webhook secret (whsec_)
    and an Anthropic key (sk-ant-api03-) were visible on screen on 2026-08-14.
    They are out of git now, but that does not revoke them. Rotation has never
    been confirmed. A live Stripe secret key can move money.
    As of 2026-08-17 the key in .env still authenticates to live Stripe, which
    says nothing either way about whether it is the original one.
    -> Rotate in the Stripe and Anthropic dashboards, then update .env.
    Detail: security.txt, finding 8.

[BLOCKER] The backend is not deployed anywhere
    There is no public API. Everything that touches the server depends on this.
    No longer blocked on tooling: Dockerfile, docker-compose.yml, Caddyfile,
    .env.example, a systemd unit and DEPLOY.md are all in
    geonix_wrench_backend/ now, and Caddy obtains the certificate itself.
    -> Follow geonix_wrench_backend/DEPLOY.md. Needs a host and a DNS record.

[BLOCKER] Release builds need an API host passed in
    Release builds have NO default and fail immediately at startup with an
    explanatory error instead of dying on every request.
    -> Build with: --dart-define=API_BASE_URL=https://your-real-api
    Still a blocker only because there is no host to point at yet.
    Debug builds no longer need this: Settings -> Development server sets the
    address at runtime.

[BLOCKER] Managed Payments opt-out is unverified against live Stripe
    Checkout previously failed with "this product tax code is ineligible for
    Managed Payments". The fix passes managed_payments={"enabled": False}
    (billing.py). It is covered by a unit test and the parameter encoding was
    checked offline, but no real checkout has ever completed.
    -> Complete one real end-to-end purchase before launch.

[BLOCKER] No app builds exist to download
    The website's download section lists App Store, Google Play, .dmg, .exe
    and .deb, all as disabled "Coming soon" placeholders.
    -> Produce the builds, then activate each row. The markup carries a
       comment showing the swap: <span class="dl"> becomes
       <a class="dl" href="..."> and the "Coming soon" line is deleted.
    Sub-tasks: Apple Developer Program membership and App Store review,
    Google Play Console listing and review, code signing and notarisation for
    the .dmg, signing for the .exe.


================================================================================
SECURITY - see security.txt for evidence and remediation
================================================================================

All seven code-side findings from security.txt are fixed. What remains needs
you:

[OWNER]  Rotate the Stripe secret key, webhook secret and Anthropic key.
[OWNER]  Purge geonix_wrench.db from git history — now urgent, see BLOCKERS.
         The repo is public and the blob is in the pushed history.
[OWNER]  Set ALLOWED_ORIGINS in the deployed environment. Until it is set the
         API allows any origin but WITHOUT credentials, and warns at startup.
         .env.example carries it as the first required value.


================================================================================
PLATFORM / SIGNING
================================================================================

macOS provisioning profile expires 2026-08-22
    The current build is signed with a free Apple ID "Personal Team" profile,
    which lasts 7 days. When it lapses, Firebase Auth fails again with
    keychain-error, because the data-protection keychain needs a
    profile-backed keychain access group.
    -> A paid Apple Developer account removes the 7-day cycle. Otherwise the
       app must be rebuilt through Xcode weekly.

Android has never been built
    google-services.json is only present as .example. The SHA-1 fingerprint
    has never been registered with Firebase, so Android auth is untested.

Windows and Linux have never been built or tested
    The site advertises .exe and .deb. Neither has been produced.

iOS has never been built or tested on a device.


================================================================================
PRODUCT GAPS
================================================================================

No custom domain
    geonixsoftware.com does not resolve. The site currently lives at
    geonixsoftware.github.io/geonix, and the app's Stripe return URLs point
    there (app_config.dart:37). Buying the domain means updating:
      - GitHub Pages custom domain settings
      - WEB_BASE_URL default in app_config.dart
      - Firebase Authentication -> Authorized domains

Seat downgrade requires manual member removal
    update_team_seats() refuses to drop below occupied seats, which is correct,
    but the owner has to go remove members elsewhere first. No guided flow.

(Account deletion and the legal pages moved to DONE on 2026-08-17.)

Password reset emails land in spam
    Confirmed during testing. Firebase's default sender has poor deliverability.
    -> Configure a custom SMTP sender and verified domain in Firebase Auth.


================================================================================
ENGINEERING DEBT
================================================================================

Flutter tests are thin
    11 tests, mostly around auth error mapping and billing status parsing.
    No widget or integration coverage of the record -> review -> PDF flow,
    which is the core product path.

The theme specimen golden is platform-sensitive
    test/theme_specimen_test.dart is tagged "golden" and can be skipped with
    flutter test --exclude-tags golden. It will fail on a different Flutter
    version or host OS. Regenerate rather than debug:
    flutter test --update-goldens test/theme_specimen_test.dart

Non-English translations are incomplete
    New billing and auth strings were only added to strings_en.dart. The other
    nine locales fall back to English at runtime, so they are not broken, but
    they are partially untranslated.

The site's pricing is hardcoded in three places
    Backend config.py is the source of truth and serves price_per_seat to the
    app, but geonix_website/geonix_wrench.html and index.html carry the
    numbers as literal text. Changing a price means editing the site too.


================================================================================
DONE - recorded so it is not redone
================================================================================

- Website rebuilt: no web checkout, no web sign-in, download-led. Pure HTML
  and CSS, no JavaScript. Live at geonixsoftware.github.io/geonix
- Dark mode across site and app, sharing one palette.
- Mobile layout verified at 320/360/375/390/414/430px, no overflow.
- Both logos (Geonix Wrench + Geonix) in the product page header, each linking
  to its own page; company mark added to the home page.
- App theme rebuilt on tonal surfaces; flicker on profile/billing refresh
  fixed; AppHeader, AudioVisualizer and RecordButton layout bugs fixed.
- Seat management: purchase and post-purchase changes, minimum 2, no maximum.
- .env, venv/ and the service account are out of git and ignored.
- Firebase Hosting deployment was taken down in favour of GitHub Pages.

Added 2026-08-15 (security + release work, no owner input needed):
- CORS locked to ALLOWED_ORIGINS; credentialed wildcard removed.
- Audio and logo uploads streamed under a byte cap, with an early
  Content-Length rejection. Audio was previously unbounded.
- Per-user rate limiting, tightest on /api/process-audio.
- /docs, /redoc and /openapi.json disabled unless ENABLE_API_DOCS=true.
- Transcript capped before the LLM prompt; PDF fields clipped, control
  characters stripped, parts table bounded.
- geonix_wrench.db untracked; *.db and *.sqlite3 ignored.
- Stripe Billing Portal: new POST /api/billing/portal-session and a
  "Manage subscription" card in the app, so cancel / change card / invoices
  no longer require emailing support.
- Webhook now handles invoice.payment_failed and invoice.payment_succeeded,
  so a subscription that lapses for non-payment reflects correctly.
- Release builds fail loudly when API_BASE_URL is missing.
- GitHub Actions CI: backend pytest, Flutter analyze + test, and a secret
  scan over both tracked files and full history.
- Test count: backend 52 -> 71, app 11.

Added 2026-08-17:
- Account deletion, end to end. DELETE /api/auth/me erases the user, their
  personal job cards and transcripts, invites in both directions and the
  subscription row; cancels the Stripe subscription first so a deleted customer
  is never billed for an account they cannot reach; removes the Firebase
  identity last, so a failure part-way is still retryable. Refuses when the
  caller owns a shop other mechanics are still in, rather than destroying their
  workplace. Settings -> Account -> Delete account, behind a confirmation.
- Privacy policy and Terms of Service written and linked: geonix_website/
  privacy.html and terms.html, in every page footer, and reachable from
  Settings -> Legal in the app. Both stores need the privacy URL; GDPR needs
  the content. THESE ARE DRAFTS — have a lawyer read them before you sell, and
  fill in the company's country of establishment in Terms -> Governing law.
- Self-hosting: Dockerfile (multi-stage, non-root, ffmpeg + libgomp1 for
  faster-whisper), docker-compose.yml with Caddy terminating TLS, Caddyfile,
  .env.example documenting all 35 environment variables, a hardened systemd
  unit, and DEPLOY.md. GET /health added, unauthenticated, for probes.
- price_check was BROKEN, not merely unrun: it called .get() on a StripeObject,
  which does not implement it, and caught stripe.error.StripeError, an alias
  removed in stripe-python 8. Both fixed, then run against live Stripe:
  Individual 29.00 EUR and Team 25.00 EUR/seat both MATCH what is advertised.
  That blocker is cleared.
- API timeouts: every backend call was unbounded except the audio upload, so an
  unreachable server left AuthGate spinning forever instead of reaching its own
  "Can't reach Geonix Wrench" screen. All 14 call sites now share a 20s budget.
- Debug-only runtime API host override (Settings -> Development server), so
  testing on a real phone no longer needs a rebuild per address change.
- iOS: UIFileSharingEnabled + LSSupportsOpeningDocumentsInPlace, so saved job
  card PDFs are reachable in the Files app instead of invisible in the
  container. NSAllowsLocalNetworking for LAN development.
- Shop logos accept SVG (kept as vector into the PDF, screened for XXE,
  scripts, event handlers and remote references) plus HEIC and the other common
  raster formats.
- Test count: backend 71 -> 150, app 11 -> 63.
